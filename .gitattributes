# Normalize all text files to LF in the repo and on checkout (matches Biome +
# Linux CI). Prevents CRLF being introduced on Windows checkouts.
* text=auto eol=lf

# Binary assets — never normalize.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
