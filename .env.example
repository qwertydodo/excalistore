# Copy to .env and fill in. .env is gitignored.

# Google Cloud OAuth client id (type: Chrome extension). Required for sign-in.
WXT_OAUTH_CLIENT_ID=

# Manifest public key (manifest.key). Optional — pins a deterministic extension
# ID across machines/reloads. Leave unset to use the project's default key.
WXT_PUBLIC_KEY=
