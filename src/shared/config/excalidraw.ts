// Single source of truth for the excalidraw.com origin — used by the content
// script match pattern, manifest host_permissions, the background's sender
// trust check, and the .excalidraw file envelope's "source" field.
export const EXCALIDRAW_ORIGIN = "https://excalidraw.com";
